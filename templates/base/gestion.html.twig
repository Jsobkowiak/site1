{% extends 'base.html.twig' %}
{% block title %}{{parent()}}Gestion{% endblock %}
{% block body %}
{% if not is_granted('IS_AUTHENTICATED_FULLY') %}
<h3 class="text-center text-danger">Pour effectuer une demande de place de cantine vous devez être connecter</h1>
    {% else %}
    {% for message in app.flashes('notice') %}
    <h2 class="alert alert-dismissible alert-success text-center mt-4 mb-4" role="alert">
        {{ message }}
    </h2>
    {% endfor %}
    <div class="container-fluid">
        <div class="row justify-content-center">

            <div class="col-10 text-primary">
                <h1 class="text-center text-primary mt-4 pt-4 display-1 fw-bold">Demande de place de cantine</h1>
                {{form(form)}}
            </div>
        </div>
    </div>
    {% endif %}
    {% endblock %}